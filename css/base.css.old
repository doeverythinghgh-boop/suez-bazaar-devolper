/*
  @file css/base.css
  @description يحتوي على الأنماط الأساسية والعامة للمشروع.
  - أنماط الجسم (Body) والخطوط.
  - أنماط الأزرار العامة.
  - مؤشر التحميل (Loader).
  - حاوية انقطاع الاتصال.
*/

/* ✅ جديد: تعريف متغيرات الألوان الرئيسية للموقع */
:root {
  --primary-color: #007bff; /* اللون الأزرق الأساسي */
  --primary-color-light: #e6f2ff; /* نسخة أفتح للتوهج والتأثيرات */
  --dark-blue: #03478f; /* درجة أغمق من الأزرق للعناوين والنصوص الهامة */
  --success-color: #28a745;
  --danger-color: #dc3545;
  --danger-color-dark: #ef4444;
  --danger-color-light: #fee2e2;
  --danger-color-lighter: #fecaca;
  --warning-color: #f59e0b;
  --info-color: #3498db;
  --secondary-color: #6c757d;

  --text-color-dark: #222222;
  --text-color-medium: #4a5568;
  --text-color-light: #7f8c8d;

  --bg-color-light: #f9f9f9;
  --bg-color-white: #ffffff;
  --border-color: #e9ecef;

  --font-primary: "Tajawal", sans-serif;
  --font-secondary: "Orbitron", sans-serif;

  --shadow-soft: 0 4px 12px rgba(0, 0, 0, 0.05);
  --shadow-focus: 0 0 0 3px rgba(59, 130, 246, 0.25);
}

/* === 1. أنماط عامة للجسم (Body) === */
body {
  margin: 0;
  background-color: var(--bg-color-light);
  color: var(--text-color-dark);
  font-family: var(--font-secondary), sans-serif;
  padding-top: 12vh;
}

/* Class to prevent scrolling when a modal is open */
body.modal-open {
  overflow: hidden;
}

/* === 2. أنماط الأزرار العامة === */
button,
.button {
  background-color: var(--primary-color);
  color: var(--bg-color-white);
  border: none;
  padding: 10px 20px;
  font-family: var(--font-primary);
  font-size: 16px;
  font-weight: bold;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease, box-shadow 0.2s ease;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px; /* Space between icon and text */
}

/* === 3. مؤشر التحميل الدائري (Spinner) === */
.loader {
  border: 5px solid var(--primary-color-light);
  border-top: 5px solid var(--primary-color);
  border-radius: 50%;
  width: 50px;
  height: 50px;
  animation: spin 1s linear infinite;
  margin: 50px auto; /* Center the loader */
}

@keyframes spin {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}

/* === 4. أنماط حاوية انقطاع الاتصال بالإنترنت === */
.offline-container {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: var(--bg-color-light);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 5000; /* للتأكد من أنها فوق كل شيء */
  text-align: center;
  flex-direction: column;
  padding: 20px;
}
.offline-content i { font-size: 4rem; color: var(--danger-color); margin-bottom: 1.5rem; }
.offline-content h2 { font-size: 1.8rem; color: var(--text-color-medium); margin-bottom: 1rem; }
.offline-content p { font-size: 1rem; color: var(--text-color-light); margin-bottom: 2rem; }
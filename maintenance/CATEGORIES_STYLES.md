# دليل التنسيق والسمات الفنية (الفئات والمنتجات) 🛠️🎨

> [!IMPORTANT]
> **ملاحظة هامة:** جميع التنسيقات والآليات المذكورة في هذا الملف مخصصة **للصفحة الرئيسية (Home Page)** أو **الصفحة الأم** فقط، وذلك لضمان تجربة مستخدم أولية مميزة وجذابة عند فتح المتجر.

هذا المستند يشرح بالتفصيل المنطق التصميمي والبرمجي الذي يحكم عرض الفئات والمنتجات في "سويس بازار"، لضمان الحفاظ على الهوية البصرية وسهولة الصيانة.

## 1. الفئات الرئيسية (Main Categories)
تعتمد على هيكل جدول (`<table>`) لضمان توزيع متساوٍ ومنظم.
- **التنسيق:** يتم عرضها في صفوف تحتوي على 4 خلايا كحد أقصى.
- **الحالة النشطة (`--active`):** 
    - يتغير لون الحدود إلى `var(--border-color-active)`.
    - يظهر ظل بؤري `var(--shadow-focus)`.
- **المؤشر البصري (Arrow):** يتم استخدام عنصر زائف (`::after`) لرسم مثلث يشير للأسفل يربط الفئة الرئيسية بصفوف التفاصيل المنبثقة منها.

## 2. الفئات الفرعية (Subcategories)
حاوية مرنة تتكيف مع كمية البيانات المعروضة.
- **الهيكل:** تستخدم نظام الشبكة (`display: grid`) مع خاصية `grid-auto-flow: column` للتوسع الأفقي.
- **المنطق الديناميكي:**
    - **صف واحد:** إذا كان عدد العناصر 5 أو أقل (كلاس `.categories_sub_one_row`).
    - **صفين:** إذا زاد العدد عن 5 (كلاس `.categories_sub_two_row`) مع تفعيل التمرير الأفقي.
- **شريط التمرير:** مصغر بسمك **3px** فقط وبألوان شفافة لتقليل التداخل البصري.
- **العناصر (Chips):** حواف دائرية (`20px`) وتغيير لوني عند الاختيار.

## 3. معرض المنتجات (Product Gallery)
الجزء الأكثر ديناميكية، صُمم ليكون سريعاً ومنظماً.
- **النمط الافتراضي:** يفتح بنمط الشبكة (`Grid View`) تلقائياً.
- **تحديد الارتفاع:** محصور بـ **3 صفوف** فقط (تقريباً `55vh` أو `480px`) لضمان عدم استهلاك مساحة الصفحة كاملة، مع تفعيل التمرير الرأسي الداخلي.
- **تزامن الألوان:** إطار المعرض يأخذ حدوداً بسمك `1px` وبلون `var(--border-color-active)` ليتطابق مع الفئة الرئيسية المفتوحة.

## 7. المواصفات الفنية الدقيقة (Design Tokens)

لضمان التطابق التام عند إضافة عناصر جديدة، يرجى الالتزام بالقيم التالية:

### أ. الحدود (Borders)
- **السمك الافتراضي:** `1px` مستمر (`solid`).
- **السمك النشط:** `2px` في بعض الحالات لتعزيز الربط، ولكن تم توحيده لـ `1px` في الحاويات المفتوحة لتقليل الثقل البصري.
- **الألوان:** 
    - الحالة العادية: `var(--border-color)` (رمادي خفيف).
    - الحالة النشطة: `var(--border-color-active)` (يتغير حسب السمة المختارة).

### ب. الظلال (Shadows)
- **الظلال البسيطة:** `var(--shadow-soft)` تستخدم للعناصر العائمة بشكل طفيف.
- **ظلال التركيز:** `var(--shadow-focus)` تستخدم للحاويات المفتوحة (الفئات الفرعية والمنتجات) لإبرازها عن بقية الصفحة.
- **التأثير:** تعتمد الظلال على قيم `rgba` لتكون متوافقة مع خلفيات الموبايل والديسكتوب.

### ج. الخطوط (Typography)
- **عائلة الخط:** `var(--font-primary)`.
- **خطوط العناوين (Categories):**
    - الفئة الرئيسية: `font-size: 0.72rem` مع `font-weight: 600`.
    - عنوان المعرض: `font-size: 0.9rem` مع `font-weight: 700`.
- **خطوط المنتجات:**
    - اسم المنتج: `font-size: 0.75rem` مع `font-weight: 600`.
- **الاصطفاف:** جميع النصوص تستخدم الـ `line-height: 1.2` لضمان مقروئية عالية في المساحات الضيقة.

### د. نظام الألوان (Color Palette)
- **الخلفيات:**
    - الصفحة العامة: `var(--bg-color-light)`.
    - تجميع التفاصيل (Grouping): `var(--bg-color-medium)` (رمادي فاتح جداً).
    - العناصر النشطة: `var(--primary-color-light)`.
- **الألوان الأساسية:**
    - النصوص والأيقونات: `var(--primary-color)`.
    - الأسماء الغامقة: `var(--text-color-dark)`.

---
> [!TIP]
> جميع هذه القيم معرفة كمتغيرات CSS في ملفات النظام الأساسية، مما يعني أن تغيير المتغير في الملف الرئيسي سينعكس تلقائياً على كل هذه الأجزاء، مما يحقق **مبدأ الحقيقة الواحدة (Single Source of Truth)**.

## 4. الربط البصري والتجميع (Visual Grouping)
لإشعار المستخدم بأن الفئات الفرعية والمنتجات جزء من نفس الأصل:
- **التظليل:** يتم إعطاء صفوف التفاصيل خلفية أغمق قليلاً `var(--bg-color-medium)`.
- **الدمج:** تم إزالة الحدود الفاصلة بين صف الفئات الفرعية وصف المنتجات، وتوحيد الحواف لتبدو كإطار واحد متصل.
- **العنوان الديناميكي:** عنصر `div` يظهر أعلى المنتجات يوضح اسم القسم النشط حالياً مع أيقونة مميزة.

## 5. الحركات والانسيابية (Animations & Transitions)
- **التمدد السلس:** استخدام `transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1)` على الخلايا لضمان نمو الإطار بنعومة.
- **دخول المنتجات (Pop-in):** أنيميشن مخصص `@keyframes categories_product_pop_in` يجعل المنتجات تظهر بتأثير "البزوغ" المتصاعد.

## 6. التجاوب مع الشاشات (Responsiveness)
تم استخدام استعلامات الوسائط (`@media`) لضمان الثبات:
- **الموبايل:** عرض الشبكة بحد أدنى `110px` لكل منتج.
- **الديسكتوب:** زيادة العرض إلى `140px` لكل منتج مع زيادة الفجوات (Gaps) لراحة العين.

---
> [!IMPORTANT]
> عند إضافة أي سمة جديدة، يرجى الالتزام بالمتغيرات اللونية (CSS Variables) لضمان توافق التصميم مع الوضع الليلي (Dark Mode).

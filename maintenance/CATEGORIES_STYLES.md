# دليل تنسيقات وتطوير قسم الفئات (Categories Maintenance Guide) 📖🏗️⚖️

هذا الدليل يشرح البناء الهندسي والجمالي المنطقي لقسم الفئات بعد "الهجرة الكبرى للشبكة".

## 1. الهيكل الهيكلي (DOM Structure)
*   **الحاوية:** `#categories_grid` (تعمل بنظام `display: grid` بـ 4 أعمدة).
*   **العنصر الرئيسي:** `.categories_grid_item` (يتحكم في تموضع الصور والنصوص).
*   **حاوية التفاصيل:** `.categories_details_container` (تأخذ `grid-column: 1 / -1` لتشغل الصف بالكامل).

## 2. ميكانيكا الالتحام (Frame Fusion) 🧪
يتم الربط باستخدام "جسر" بكسلي (`::after`) وعناصر منحنيات مقعرة (`Dramatic Inverted Radius 25px`):
- **الجسر المركزي:** يغطي الفجوة (Gap) ويتداخل بمقدار `2px` إضافية لمحو الحد العلوي للحاوية تماماً.
- **المنحنيات المقعرة:** تُنشأ باستخدام `box-shadow` بريديس 25 بكسل، لترسم حدوداً موحدة (1px) ومستمرة تصل الفئة بالحاوية كخط واحد منساب.
- **الاستمرارية:** تضمن القواعد المطبقة ظهور الحدود الخارجية للفئة والحاوية كخط واحد منحني ومستمر دون أي نتوءات أو فواصل أفقية.

## 3. ميكانيكا التموضع (Insertion Logic)
يتم إدراج التفاصيل برمجياً في `categories.js` باستخدام الدالة `categories_toggleSubcategoriesGrid`:
1.  نحسب مكان العنصر المختار.
2.  نحدد آخر عنصر في "الصف" الذي ينتمي إليه (بناءً على 4 أعمدة).
3.  نستخدم `.after()` لإدراج حاوية التفاصيل، مما يضمن ثبات العناصر الرئيسية الأخرى.

## 4. هندسة الالتحام (Fusion Engineering)
*   **الجسر (`::after`):**
    ```css
    bottom: -17px;
    height: 37px;
    background-color: var(--bg-color-medium);
    ```
    تم حساب هذه القيم لتعويض `padding-bottom` (20px) + `grid-gap` (15px) + تداخل (2px).
*   **التداخل العلوي:** الحاوية السفلية تستخدم `margin-top: -2px` لتلتصق تماماً بالجسر.

## 4. ثوابت التصميم المستعادة (Aesthetic Tokens)
*   **الخطوط:** الدقة الموحدة هي `0.85rem` لجميع النصوص (فئات، منتجات).
*   **الوزن:** `font-weight: 600` لجميع العناوين.
*   **التباين:**
    - `.categories_details_container`: خلفية بيضاء (`--bg-color-light`).
    - `.categories_subcategory_item`: خلفية رمادية (`--bg-color-medium`).
*   **الصور (Products):** الحجم الثابت هو `120px` مع `object-fit: contain`.

## 5. حالة النشاط (Active State)
*   تعريف الحالة بالدالة `classList.add("categories_grid_item--active")`.
*   يجب إزالة الكلاس من جميع العناصر قبل تفعيله في الجديد (أو إزالته عند الإغلاق).

---
**تحذير صيانة:** عند تغيير عدد الأعمدة في CSS، يجب تحديث ثابت `columns = 4` في ملف `categories.js` لضمان بقاء الاستقرار الهيكلي.

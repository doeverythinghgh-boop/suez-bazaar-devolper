# هذا اللمف شرح لجسور التواصل بين التطبيق و الموقع المحلي الذي يتم عرضة في الويب فيو 

# توثيق الجسر البرمجي ونظام أندرويد (Android Interface Bridge)

يعتمد تطبيق "بازار" على جسر برمجي (JavaScript Interface) للربط بين بيئة الويب (WebView) ونظام أندرويد الأصلي (Native). يتم الوصول إلى وظائف أندرويد عبر الكائن العالمي `window.Android`.

---

## 🔗 1. من الويب إلى أندرويد (Web to Native)
يتم استدعاء هذه الدوال من داخل كود جافا سكريبت للتأثير على تطبيق الأندرويد. جميع الدوال تتبع البادئة `window.Android.*`.

### جدول الدوال السريع
| الدالة                          | المعاملات                     | الغرض                                                | مكان الاستدعاء في الويب    |
| :------------------------------ | :---------------------------- | :--------------------------------------------------- | :------------------------- |
| `onUserLoggedIn`                | `userKey`                     | إبلاغ أندرويد بتسجيل الدخول لبدء توليد FCM Token.    | `notificationSetUp.js`     |
| `onUserLoggedOut`               | `userKey`                     | إبلاغ أندرويد بتسجيل الخروج لمسح التوكن والاشتراكات. | `notificationTools.js`     |
| `requestNotificationPermission` | -                             | طلب إذن الإشعارات من نظام أندرويد (خاصة 13+).        | `notificationTools.js`     |
| `onNotificationsEnabled`        | -                             | إبلاغ أندرويد بتفعيل الإشعارات يدوياً من الويب.       | `notifications-actions.js` |
| `onNotificationsDisabled`       | -                             | إبلاغ أندرويد بتعطيل الإشعارات يدوياً من الويب.       | `notifications-actions.js` |
| `onLanguageChanged`             | `lang` ('ar'/'en')            | مزامنة لغة التطبيق الأصلية مع لغة الويب.             | `index.js`                 |
| `checkForUpdates`               | -                             | طلب فحص وجود تحديثات للتطبيق (Play Store).           | `index.js`                 |
| `sendNotificationsToTokensP2P`  | `tokensJson`, `title`, `body` | إرسال إشعار مباشر (Direct Push) من الجهاز.           | `notificationTools.js`     |

### تفاصيل الدوال
#### `onUserLoggedIn(userKey)`
- **الغرض**: إبلاغ تطبيق أندرويد بأن المستخدم قد سجل دخوله بنجاح.
- **التأثير**: يقوم التطبيق ببدء عملية توليد توكن Firebase (FCM) وتخزينه.
- **الاستدعاء**: يتم آلياً في `setupFirebaseAndroid` داخل ملف `notificationSetUp.js`.

#### `onUserLoggedOut(userKey)`
- **الغرض**: إبلاغ التطبيق بتسجيل الخروج.
- **التأثير**: يقوم التطبيق بمسح بيانات الجلسة وإيقاف استقبال الإشعارات المرتبطة بهذا المستخدم.
- **الاستدعاء**: يتم في دالة `onUserLoggedOutAndroid` داخل `notificationTools.js`.

#### `requestNotificationPermission()`
- **الغرض**: طلب إذن الإشعارات من نظام أندرويد (خاصة لإصدارات 13 فما فوق).
- **الاستدعاء**: دالة `askForNotificationPermission` في `notificationTools.js`.

#### `sendNotificationsToTokensP2P(tokensJson, title, body)`
- **الغرض**: إرسال إشعارات مباشرة من الجهاز (Direct Push) دون المرور بالخادم في حالات معينة.
- **المعاملات**: 
  - `tokensJson`: مصفوفة التوكنات بتنسيق JSON String.
  - `title`: عنوان الإشعار.
  - `body`: نص الإشعار.
- **الاستدعاء**: دالة `sendNotificationsToTokens` في `notificationTools.js`.

#### `onLanguageChanged(lang)`
- **الغرض**: إبلاغ تطبيق أندرويد بتغيير لغة التطبيق لتحديث واجهاته وتخزين التفضيل محلياً.
- **المعاملات**: `lang` (String): رمز اللغة الجديد (`'ar'` أو `'en'`).
- **الاستدعاء**: يتم استدعاؤها تلقائياً عند تغيير اللغة في دالة `toggleAppLanguage` داخل `index.js`.

---

## 📡 2. من أندرويد إلى الويب (Native to Web)
يتم استدعاء هذه الدوال من كود أندرويد (Java/Kotlin) للتفاعل مع واجهة الويب.

### `saveNotificationFromAndroid(notificationJson)`
- **الغرض**: استقبال بيانات الإشعار الذي وصل للجهاز وحفظه في السجل المحلي للمتصفح.
- **المعاملات**: سلسلة نصية (JSON) تحتوي على بيانات الرسالة.
- **الموقع**: معرفة عالمياً في `notificationTools.js`.
- **المنطق**: تقوم بتحليل الـ JSON واستدعاء `addNotificationLog` لحفظه في IndexedDB.

### `showNotificationsModal()`
- **الغرض**: فتح شاشة الإشعارات داخل تطبيق الويب مباشرة.
- **الاستخدام**: تُستدعى غالباً عند النقر على إشعار في شريط التنبيهات بأندرويد لفتح الواجهة المناسبة للمستخدم.

---

## 🛠 3. دورة حياة التوكن في أندرويد (FCM Lifecycle)
1. يفتح المستخدم التطبيق -> يتم فحص وجود `android_fcm_key` في `localStorage`.
2. إذا لم يوجد -> يتم استدعاء `window.Android.onUserLoggedIn`.
3. تطبيق أندرويد يولد التوكن -> يقوم بحقنه في الـ `localStorage` للمتصفح.
4. الويب ينتظر التوكن عبر دالة `waitForFcmKey` في `notificationSetUp.js`.
5. بمجرد استلامه، يتم إرساله للخادم عبر `sendTokenToServer` لربطه بحساب المستخدم.

---

## 💾 4. مفاتيح التخزين المشتركة (Shared Storage)
يجب على مطور أندرويد التعامل مع هذه المفاتيح في `localStorage`:

- `android_fcm_key`: التوكن الذي يضعه أندرويد ليقرأه الويب.
- `notifications_enabled`: ('true'/'false') حالة الإشعارات الحالية.
- `app_language`: ('ar'/'en') اللغة المختارة.
- `theme`: ('dark'/'light') المظهر المختار.

---

## 🎨 5. تعديلات واجهة المستخدم في بيئة أندرويد
عند اكتشاف `window.Android` (بيئة التطبيق الأصلي)، يقوم الويب بـ:
1. **إخفاء PWA Install**: منع ظهور رسائل تثبيت تطبيق الويب (لأن المستخدم في التطبيق فعلاً).
2. **كتم الصوت**: تعطيل `playNotificationSound()` في الويب لأن أندرويد يتولى التنبيه الصوتي.
3. **تعديل الروابط**: فتح الروابط الخارجية في المتصفح الافتراضي بدلاً من الـ WebView.

---

## 💡 ملاحظات تقنية للمطورين
- **فحص الواجهة**: يجب دائماً فحص وجود الواجهة قبل الاستدعاء لتجنب الأخطاء في المتصفحات العادية:
  ```javascript
  if (window.Android && typeof window.Android.functionName === 'function') {
      // Call Android
  }
  ```
- **التوافقية**: تعتمد هذه الوظائف على تعريف `WebView.addJavascriptInterface` في كود أندرويد باسم `"Android"`.
- **تصحيح الأخطاء**: تظهر رسائل التصحيح في Logcat الخاص بأندرويد ببادئة `[FCM Android]` أو `[Dev]`.

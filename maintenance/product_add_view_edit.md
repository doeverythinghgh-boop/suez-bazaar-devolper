
# توثيق وحدة إضافة المنتج (ProductAdd - Add1)

تعتبر وحدة `productAdd` المسؤولة عن واجة إضافة المنتجات الجديدة للنظام (Add1). تعتمد على نفس الفلسفة المعيارية لسهولة التطوير.

## 📁 هيكل المجلد
المسار: `pages/productAdd/`

- **`productAdd.css`**: التصميمات الخاصة بواجهة الإضافة، بما في ذلك منطقة رفع الصور التفاعلية.

### 📂 مجلد البرمجيات (js/)
المنطق البرمجي مقسم إلى 5 ملفات:

1. **`add1_config.js`**:
   - الثوابت: أبعاد الضغط (1600px)، الجودة (0.75)، والحد الأقصى (6 صور).
   - الحالة: مصفوفة الصور `add1_images` وعلم المعالجة `isProcessingFilesAdd1`.

2. **`add1_utils.js`**:
   - أدوات عرض الأخطاء المخصصة (`add1_showError`).
   - دوال التنسيق المساعدة وتوليد المعرفات الفريدة للصور.

3. **`add1_image.js`**:
   - معالجة الصور: فحص دعم `WebP` وتنفيذ الضغط المتوافق مع ذاكرة الهواتف.
   - مدير الملفات الجديدة (`add1_handleNewFiles`) الذي ينظم عملية التحقق والضغط.

4. **`add1_ui.js`**:
   - إدارة عناصر DOM ومستمعي الأحداث.
   - إنشاء معاينات الصور (`add1_createPreviewItem`) وحذفها.
   - تشغيل الكاميرا عبر واجهة النظام (Capture Environment) للموبايل.
   - عدادات الحروف اللحظية وفلاتر الحقول الرقمية.

5. **`add1_submit.js`**:
   - التحقق من صحة جميع الحقول (6 تحققات أساسية).
   - إدارة حالة زر الإرسال (`add1_setSubmitLoading`).
   - توليد الرقم التسلسلي، رفع الصور للسحابة، وحفظ البيانات النهائية.
   - إخطار الإدارة بإضافة المنتج الجديد.

## ⚙️ سير العمل (Workflow)
1. يتم اختيار الصور أو التقاطها.
2. يتم ضغط الصور فوراً في الخلفية لعرض حجمها النهائي وتقليل وقت الرفع.
3. عند الإرسال، يتم التأكد من اكتمال كافة البيانات.
4. يتم رفع الصور إلى Cloudflare R2 والحصول على الروابط.
5. يتم حفظ السجل في قاعدة البيانات وإرسال إشعار فوري للإدارة.

---

# توثيق وحدة إضافة الخدمة (ProductAdd2 - Add2)

تعتبر وحدة `productAdd2` المسؤولة عن واجهة إضافة الخدمات (Add2). تم تصميمها لتقديم تجربة مستخدم سلسة مع دعم متطور للوسائط المتعددة.

## 🧱 البنية الهيكلية (HTML)
المسار: `pages/productAdd2/productAdd2.html`

البنية تعتمد على عناصر (Modal) تضم نموذجاً (`form`) يحتوي على الأقسام التالية:
- **منطقة رفع الصور**: تدفع المستخدم لاختيار ملفات أو استخدام الكاميرا.
- **حقول البيانات**: تشمل (اسم الخدمة، وصف الخدمة، رسالة مقدم الخدمة، الملاحظات الخاصة).
- **التذييل**: يحتوي على زر الحفظ والنشر.
- **وعاء الكاميرا**: عنصر مخفي (`add2_camera_modal_container`) يظهر عند تفعيل ميزة التصوير المكتبي.

## 🎨 التنسيق البصري (CSS)
المسار: `pages/productAdd2/productAdd2.css`

- يتميز التصميم باستخدام تدرجات الألوان الصلبة (Flat Colors) واللون الأزرق (`#3498db`) كعنصر هوية أساسي.
- استخدام نظام الشبكة (`Grid`) لعرض معاينات الصور بشكل متجاوب.
- دعم كامل لحالة الـ `active` بدلاً من الـ `hover` لضمان توافقية اللمس.
- تصميم متجاوب (Responsive) يغير حجم الخطوط والحشو بناءً على عرض الشاشة.

## 🧠 المنطق البرمجي (JavaScript)

### 1. الإعدادات العامة (`add2_config.js`)
- تحديث قيم الثوابت لضغط الصور والحد الأقصى للملفات (6 صور).
- إدارة مصفوفة `add2_images` التي تخزن حالات الصور (pending, compressing, ready, error).

### 2. الأدوات والوظائف الأساسية (`add2_utils.js`)
- **`add2_showError(element, message)`**: حقن رسائل الخطأ ديناميكياً تحت الحقول.
- **`add2_clearError(element)`**: تنظيف رسائل الخطأ.
- **`add2_formatBytes(bytes)`**: تحويل أحجام الملفات لنصوص مقروءة.
- **`add2_genId()`**: توليد معرفات فريدة لعناصر المعاينة.

### 3. معالجة الصور المتطورة (`add2_image.js`)
- **`add2_compressImage(file)`**: تستخدم `createImageBitmap` لتحجيم الصور بذكاء مع مراعاة موارد ذاكرة الهواتف. التحويل يتم لـ `webp` أو `jpeg`.
- **`add2_handleNewFiles(fileList)`**: المنسق الرئيسي الذي يستقبل الملفات، يتحقق من المساحة المتوفرة، ويطلق عملية الضغط المتوازي.

### 4. واجهة المستخدم والتصوير المباشر (`add2_ui.js`)
- **`add2_createPreviewItem(state)`**: بناء عناصر المعاينة مع أزرار الحذف وعرض التقدم.
- **`add2_removeImage(id)`**: حذف الصور مع تأكيد عبر `Swal`.
- **`add2_openDesktopCamera()`**: تشغيل بث الفيديو المباشر في (Modal) والتقاط الصور عبر `Canvas`.
- **مستمعي الأحداث**: إدارة عدادات الحروف اللحظية وفلترة المدخلات الرقمية.

### 5. التحقق والحفظ النهائي (`add2_submit.js`)
- **`add2_setSubmitLoading(isLoading)`**: تغيير حالة زر الإرسال بصرياً ومنع النقر المزدوج.
- **معالج الـ `submit`**:
    - تنفيذ 6 اختبارات منطقية (وجود صور، الاسم، طول الوصف، إلخ).
    - رفع الصور المضغوطة لـ Cloudflare R2 والحصول على الروابط النهائية.
    - تجميع كائن البيانات وإرساله لـ `addProduct`.
    - إخطار الإدارة والقيام بعملية تنظيف (Reset) شاملة للنموذج بعد النجاح.

## 💡 ملاحظات برمجية عامة
- تم الالتزام ببادئة `add2_` لجميع المتغيرات والدوال وعناصر الـ ID لضمان عزل النطاق العام.
- جميع العمليات الثقيلة (التحويل والرفع) تتم مع إظهار حالات تحميل (Loading) واضحة للمستخدم.


# توثيق وحدة تعديل المنتج (ProductEdit)

تعتبر وحدة `productEdit` المسؤولة عن واجهة تعديل المنتجات والخدمات الحالية في النظام. تم تصميمها لتكون معيارية (Modular) ومنظمة بشكل يسهل صيانتها.

## 📁 هيكل المجلد
المسار: `pages/productEdit/`

- **`productEdit.html`**: يحدد بنية النموذج (Form) وحقول الإدخال، ومنطقة رفع الصور، ووسوم السكربت للملفات المعيارية.
- **`productEdit.css`**: يحتوي على التصاميم العصرية للنموذج، وشبكة معاينة الصور، والتنسيقات المتجاوبة مع الأجهزة المختلفة.

### 📂 مجلد البرمجيات (js/)
تم تقسيم المنطق البرمجي إلى 6 ملفات أساسية:

1.  **`edit_config.js`**:
    - إدارة الإعدادات الثابتة لضغط الصور (العرض الأقصى 1600px، الجودة 0.75).
    - إدارة الحالة العامة (Global State) مثل مصفوفة الصور الحالية وأسماء الصور الأصلية.
    - يدعم كائن `window.productModule` للتوافقية.

2.  **`edit_utils.js`**:
    - دوال مساعدة لعرض وإزالة رسائل الخطأ (`EDIT_showError`, `EDIT_clearError`).
    - دالة تحويل أحجام الملفات لنص مقروء (`EDIT_formatBytes`).
    - توليد معرفات فريدة لعناصر المعاينة (`EDIT_genId`).

3.  **`edit_image.js`**:
    - فحص دعم المتصفح لتنسيق `WebP`.
    - معالجة ضغط الصور باستخدام `Canvas` لتحسين الأداء وتقليل حجم الرفع.
    - تحميل الصور الموجودة مسبقاً من Cloudflare R2 عند فتح صفحة التعديل.
    - معالجة الملفات الجديدة المضافة وفلترتها.

4.  **`edit_ui.js`**:
    - إدارة أحداث واجهة المستخدم (اختيار الملفات، تشغيل الكاميرا).
    - إنشاء عناصر المعاينة (Previews) ديناميكياً وإدارتها.
    - تنفيذ ميزة الكاميرا لسطح المكتب (Webcam Capture).
    - عدادات الحروف اللحظية للحقول النصية والتحقق من صحة المدخلات الرقمية.

5.  **`edit_submit.js`**:
    - معالج إرسال النموذج (Submit Handler).
    - التحقق من صحة جميع البيانات (Validation) قبل البدء بالرفع.
    - اكتشاف التغييرات (Change Detection) لمنع الحفظ إذا لم يتم تعديل أي بيانات.
    - رفع الصور الجديدة إلى Cloudflare وحذف الصور التي تمت إزالتها.
    - تحديث قاعدة البيانات وإعادة ضبط حالة الموافقة لـ "قيد الانتظار".

6.  **`edit_init.js`**:
    - المحرك الأساسي لتهيئة الصفحة.
    - استخراج بيانات المنتج من `productSession` وتوزيعها على الحقول المناسبة.
    - تفعيل الأحداث الأولية لضمان عمل العدادات بشكل صحيح فور التحميل.

## 🛠 التبعيات الخارجية
تعتمد الوحدة على الأدوات التالية:
- **SweetAlert2 (Swal)**: للرسائل التفاعلية وتأكيدات الحذف.
- **FontAwesome**: للأيقونات.
- **Cloudflare R2 APIS**: لرفع وحذف الملفات.
- **Internal Database APIs**: مثل `updateProduct` و `notifyAdminOnItemUpdate`.

---

# توثيق وحدة تعديل الخدمة (ProductEdit2 - Edit2)

تعتبر وحدة `productEdit2` النسخة المعيارية المخصصة لتعديل الخدمات الموجودة مسبقاً. تم تقسيمها إلى 6 ملفات برمجية لضمان سهولة التوسع.

## 📁 هيكل المجلد
المسار: `pages/productEdit2/`

- **`productEdit2.html`**: الهيكل الذي يضم حقول الخدمة (بدون سعر أو كمية).
- **`productEdit2.css`**: التنسيقات البصرية للنموذج ومعاينات الصور.

### 📂 مجلد البرمجيات (js/)
1. **`edit2_config.js`**:
   - إعدادات الضغط (1600px).
   - الحالة المشتركة لـ `window.productModule` لضمان التوافقية مع الأنظمة القديمة.

2. **`edit2_utils.js`**:
   - إدارة رسائل الخطأ (`EDIT2_showError`).
   - توليد المعرفات الفريدة وتنسيق أحجام الملفات.

3. **`edit2_image.js`**:
   - `EDIT2_loadExistingImages`: جلب الصور الحالية من السحابة عند البدء.
   - منطق ضغط الصور الذكي للخدمات.

4. **`edit2_ui.js`**:
   - إدارة التفاعل مع حقول النص (الاسم، الوصف، الرسالة) وتحديث العدادات.
   - ميزة الكاميرا لسطح المكتب والموبايل.
   - إنشاء وحذف عناصر المعاينة.

5. **`edit2_submit.js`**:
   - التحقق من التعديلات (Change Detection).
   - رفع الصور الجديدة وحذف القديمة من R2.
   - تحديث السجل في قاعدة البيانات كـ `serviceType: 2`.

6. **`edit2_init.js`**:
   - دالة التهيئة `initializeEditProductForm` التي تملأ البيانات فور فتح الصفحة.

## 💡 ملاحظات فنية
- الوحدة تفرض نوع الخدمة (2) دائماً في عملية التحديث.
- يتم تصفير حالة الموافقة (`is_approved: 0`) تلقائياً عند أي تعديل لإعادة فحصها من قبل الإدارة.
- الوفرة البرمجية: جميع الدوال تستخدم البادئة `EDIT2_` لضمان استقرار البيئة البرمجية وعدم التداخل مع وحدات الإضافة.

---

# توثيق وحدة عرض المنتج (ProductView)

المسؤولة عن عرض تفاصيل المنتجات العادية مع إمكانية تحديد الكمية والإضافة للسلة.

## 📁 هيكل المجلد
المسار: `pages/productView/`

### 📂 مجلد البرمجيات (js/)
1. **`view_config.js`**: يدير الحالة المحلية للواجهة (UI State).
2. **`view_utils.js`**: يحتوي على دالة `productView_updateTotalPrice` لحساب الإجمالي لحظياً.
3. **`view_ui.js`**:
   - `productView_getDomElements`: جلب عناصر الـ DOM لضمان عدم وجود مراجع قديمة.
   - `productView_populateThumbnails`: إنشاء معرض الصور التفاعلي.
   - `productView_setupQuantityControls`: إدارة أزرار الزيادة والنقصان والتحقق من الحد الأقصى.
   - `productView_setupAddToCart`: ربط بيانات المنتج بمنطق السلة العالمي.
4. **`view_init.js`**:
   - `productView_viewDetails`: الدالة المركزية التي تنظم توزيع البيانات على الواجهة وتطبق منطق الأدوار (إخفاء/إظهار بيانات البائع والمسؤول).

---

# توثيق وحدة عرض الخدمة (ProductView2)

المسؤولة عن عرض الخدمات المتطورة باستخدام سلايدر ثلاثي الأبعاد مع نظام طلب تصوير خاص.

## 📁 هيكل المجلد
المسار: `pages/productView2/`

### 📂 مجلد البرمجيات (js/)
1. **`view2_config.js`**: الثوابت الخاصة بالسلايدر، ضغط الصور، ومصفوفة الصور المرفقة للطلب.
2. **`view2_utils.js`**: `pv2_compressImage` لضغظ ورفع صور "طلب التصوير" بجودة عالية مع تحويلها لـ WebP.
3. **`view2_slider.js`**: منطق محرك السلايدر ثلاثي الأبعاد بالكامل، بما في ذلك حساب المصفوفات للحركة والتشغيل التلقائي.
4. **`view2_ui.js`**: إدارة معاينات الصور المرفقة والتحكم في عناصر واجهة طلب الخدمة.
5. **`view2_submit.js`**: `pv2_sendOrder` المسؤول عن الرفع لـ R2، إنشاء السجل في قاعدة البيانات، وإرسال الإشعارات.
6. **`view2_init.js`**: دالة التهيئة `pv2_viewDetails` ومنطق ربط الأحداث الأولي للنموذج.

## 💡 ملاحظات فنية
- تعتمد الوحدات على `ProductStateManager` لضمان تدفق البيانات بشكل آمن بين الصفحات.
- يتم تطبيق منطق حماية الأدوار (Roles Check) لمنع تسريب بيانات التكلفة أو هوية البائع لغير المصرح لهم.
